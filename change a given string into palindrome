#include<stdio.h>
#include <string.h>


int main()
{
    char arr[50];
    int l,i;
    printf("enter");
    scanf("%s",&arr);
    l=strlen(arr);
    for(i=0;i<l;i++)
    {
        if(arr[i]!=arr[l-i-1])
        {
            arr[l-i-1]=arr[i];
        }
    }
    printf("%s",arr);
}
